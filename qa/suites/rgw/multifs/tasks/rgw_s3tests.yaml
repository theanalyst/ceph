tasks:
- install:
- ceph:
- rgw: [client.0]
- s3tests:
    client.0:
      git_remote: git://github.com/theanalyst/
      force-branch: wip-lc-tagging
      rgw_server: client.0
overrides:
  ceph:
    conf:
      client:
        rgw lc debug interval: 10
